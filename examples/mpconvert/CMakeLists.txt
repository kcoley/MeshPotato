FILE(GLOB MPCONVERT_SOURCE "*.C")

ADD_EXECUTABLE(mpconvert ${MPCONVERT_SOURCE})
TARGET_LINK_LIBRARIES(mpconvert meshpotato 
	${OPENVDB_LIBRARIES}
        ${Boost_LIBRARIES}
	${OPENIMAGEIO_LIBRARIES}
	${ALEMBIC_LIBRARIES}
)
INSTALL(TARGETS mpconvert DESTINATION bin)
