FIND_PACKAGE(Boost COMPONENTS ${Boost_NEEDED_LIBS} REQUIRED)
FIND_PACKAGE(PythonLIBS)
FIND_PACKAGE(Alembic)
FIND_PACKAGE(Half)
#FIND_PACKAGE(HDF5)
SET(Boost_NEEDED_LIBS filesystem
                      python
                      thread
                      system
                      )
SET(Boost_USE_STATIC_LIBS     OFF)
SET(Boost_USE_MULTITHREADED    ON)
SET(Boost_USE_STATIC_RUNTIME     OFF)

SET(PLUGIN_INCLUDE_DIRS ${CMAKE_SOURCE_DIR}/include
      ${Boost_INCLUDE_DIRS}
      ${PYTHON_INCLUDE_PATH}
      ${ALEMBIC_INCLUDE_DIRS}
      ${HALF_INCLUDE_DIRS}
 #     ${HDF5_INCLUDE_DIR}
  )
SET(PLUGIN_LIBRARY_DIRS meshpotato
      ${PYTHON_LIBRARIES}
      ${Boost_LIBRARIES}
      ${ALEMBIC_LIBRARIES}
      ${HALF_LIBRARIES}
  )
INCLUDE_DIRECTORIES(${PLUGIN_INCLUDE_DIRS})

SET(ABCOUTPUT_SRC ABCOutputPlugin.C MeshData.C)
#SET(OBJINPUT_PYTHON_SRC pyOBJInputPlugin.C)

ADD_LIBRARY(abcoutput SHARED ${ABCOUTPUT_SRC})
TARGET_LINK_LIBRARIES(abcoutput ${PLUGIN_LIBRARY_DIRS})
INSTALL(TARGETS abcoutput DESTINATION lib)

#ADD_LIBRARY(pyOBJInputPlugin MODULE ${OBJINPUT_PYTHON_SRC})
#SET_TARGET_PROPERTIES(pyOBJInputPlugin PROPERTIES PREFIX "")
#SET_TARGET_PROPERTIES(pyOBJInputPlugin PROPERTIES SUFFIX ".so")
#TARGET_LINK_LIBRARIES(pyOBJInputPlugin meshpotato ${PLUGIN_LIBRARY_DIRS})
#INSTALL(TARGETS pyOBJInputPlugin DESTINATION pymeshpotato)
